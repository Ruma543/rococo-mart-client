import React from 'react';
import { useLoaderData } from 'react-router-dom';

const Review = () => {
  // const review = useLoaderData();
  // console.log(review);
  return <div>{/* <h3>{review.length}</h3> */}</div>;
};

export default Review;
